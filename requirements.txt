# PhysicsForge Core Dependencies
# These are the MINIMAL dependencies required for basic functionality
# Most core scripts use only Python standard library

# Core parsing and validation (required for equation extraction pipeline)
lark>=1.1.2
jsonschema>=4.19,<5
bibtexparser>=1.4.0,<2.0.0
