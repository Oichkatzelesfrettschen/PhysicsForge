## Summary

Describe what this PR changes and why.

## Verification

- [ ] `make ascii_guard` passes
- [ ] `make test` passes
- [ ] `make pipeline` runs (optional deps OK to skip)
- [ ] `make latex_strict` passes (for LaTeX changes)
- [ ] `make reports` generated updated artifacts

## Artifacts

List any regenerated files (e.g., `equation_catalog.csv`, `CATALOG_PARITY.md`).

## Screenshots (if applicable)

Include screenshots for LaTeX layout changes.

