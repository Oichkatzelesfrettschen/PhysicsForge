diff a/synthesis/scripts/compile_strict.sh b/synthesis/scripts/compile_strict.sh	(rejected hunks)
@@ -1,8 +1,12 @@
 #!/usr/bin/env bash
 set -euo pipefail
 
-cd "$(dirname "$0")"
-MAIN="../main.tex"
+# Change to the synthesis directory
+cd "$(dirname "$0")/.."
+
+MAIN="main.tex"
+LOG="main.log"
+BLG="main.blg"
 
 if ! command -v pdflatex >/dev/null 2>&1 && ! command -v latexmk >/dev/null 2>&1; then
   echo "pdflatex/latexmk not found. Install TeX Live or MiKTeX." >&2
@@ -12,11 +16,12 @@ fi
 if command -v latexmk >/dev/null 2>&1; then
   latexmk -pdf -interaction=nonstopmode "$MAIN"
 else
+  pdflatex -interaction=nonstopmode "$MAIN"
+  bibtex main.aux
   pdflatex -interaction=nonstopmode "$MAIN"
   pdflatex -interaction=nonstopmode "$MAIN"
 fi
 
-LOG="../main.log"
 if [[ -f "$LOG" ]];
 then
   if grep -Eqi "^!|LaTeX Warning|Package .* Warning" "$LOG"; then
@@ -25,4 +30,14 @@ then
   fi
 fi
 
+if [[ -f "$BLG" ]];
+then
+  if grep -q "Warning--I didn't find a database entry for" "$BLG"; then
+    echo "::error:: FATAL: Missing BibTeX entries found." >&2
+    echo "Build failed due to 'warnings as errors' policy." >&2
+    echo "Check '$BLG' for the full list of missing keys." >&2
+    exit 1
+  fi
+fi
+
 echo "Strict compile passed (no warnings)."
